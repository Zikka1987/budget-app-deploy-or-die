services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: saldohelt-backend:compose
    container_name: saldohelt-backend
    env_file:
      - .env.server
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
